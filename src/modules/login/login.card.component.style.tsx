import styled from "styled-components";
import { BrandColor, Radius } from "../../components/constants";

export const CardLoginStyled = styled.div`
  max-width: 500px;
  padding: 20px;
  background-color: ${BrandColor.White};
  border-radius: ${Radius.Large};
`;
